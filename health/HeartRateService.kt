package com.pixelwatch.health

class HeartRateService {
    fun processHeartRateData(value: Int) {
        // Handle heart rate sample (update dashboard, send to paired phone, etc)
    }
    fun syncHeartRate(value: Int) {
    // Stream to paired phone using protocol format
}
}
